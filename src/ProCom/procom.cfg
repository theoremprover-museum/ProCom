%% ATTENTION: This file has been generated automatically.
%%            Changes to this file may be overwritten.
%%            Consult INSTALL and Makefile for details.
%%
%% Last created: Thu Jul 6 11:04:25 MET DST 1995
%%      by user: gerd
%%      on host: upsilon
%%
%----------------------------------------------------------
%- This file contains declaration for provers which will
%- be loaded under the control of ProCom/Capri.
%-
%- The order is relevant when automatically selecting an
%- appropriate prover.
%-
define_prover(extension_procedure).
define_prover(me_para).

%----------------------------------------------------------
%- Declare the search strategies to be loaded.
%-
define_search(depth_first).
define_search(iterative_deepening).
define_search(iterative_inferences).
define_search(iterative_widening).
define_search(iterative_broadening).

%----------------------------------------------------------
%- Declare the reorder strategies to be loaded.
%-
define_reorder(simple).

